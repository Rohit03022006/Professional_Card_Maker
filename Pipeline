pipeline {
    agent any 
    
    stages {
        stage('Code') {
            steps {
                echo 'Cloning GitHub repo…'
                git branch: 'main',
                    url: 'https://github.com/Rohit03022006/Professional_Card_Maker.git'
            }
        }
        
        stage('Build and Test') {
            steps {
                echo 'Building Docker image...'
                sh 'docker build -t rohitxten/professional_card:latest .'
            }
        }
        
        stage('Push') {
            steps {
                echo 'Pushing the Docker image to DockerHub'
                withCredentials([usernamePassword(
                    credentialsId: 'Dockerhub',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh '''
                        echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin
                        docker push rohitxten/professional_card:latest
                    '''
                }
            }
        }
        
        stage('Deploy') {
            steps {
                echo 'Deploying application...'
                sh 'docker compose down && docker compose up -d'
            }
        }
    }         

    post {
        failure {
            echo 'Pipeline failed! Check logs for errors.'
        }
        success {
            echo 'Pipeline succeeded! Image pushed and app deployed.'
        }
    }
}  
